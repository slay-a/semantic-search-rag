# Base Streamlit theme — locked to dark.
[theme]
base = "dark"
primaryColor = "#818cf8"
backgroundColor = "#0a0a0c"
secondaryBackgroundColor = "#141418"
textColor = "#ececf1"
font = "sans serif"

[server]
headless = true
