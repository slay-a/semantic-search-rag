# Copy to .env and fill in. .env is git-ignored.

# Claude / Anthropic — used for answer generation (Messages API).
# https://platform.claude.com/
ANTHROPIC_API_KEY=sk-ant-...

# Voyage AI — used for text embeddings (Anthropic's recommended embeddings provider).
# https://www.voyageai.com/
VOYAGE_API_KEY=pa-...

# ---- Optional overrides (defaults live in src/rag_pipeline/config.py) ----
# RAG_GENERATION_MODEL=claude-opus-4-8
# RAG_EMBEDDING_MODEL=voyage-3
# RAG_EMBEDDING_PROVIDER=voyage          # voyage | hash (hash = offline, no key, for tests/demos)
# RAG_CHUNK_TOKENS=512
# RAG_CHUNK_OVERLAP=64
# RAG_TOP_K=5
# RAG_EFFORT=high                        # low | medium | high | xhigh | max
# RAG_STORE_PATH=store/index.npz
